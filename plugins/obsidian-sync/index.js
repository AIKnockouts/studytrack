// Obsidian Vault Sync — scaffold
// Drop this folder into ~/.studytrack/plugins/ to install.
// When complete, this plugin will write completed sessions to Obsidian daily notes.
module.exports = {
  onLoad() {
    // TODO: prompt for vault path, then:
    // api.on('session:completed', (session) => { ... append to daily note ... })
  },
  onUnload() {},
}
